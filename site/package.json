{
  "name": "feed-cleaner-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "zip": "node scripts/zip-extension.mjs",
    "dev": "npm run zip && wrangler dev",
    "deploy": "npm run zip && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@supabase/server": "^1.8.0"
  }
}
